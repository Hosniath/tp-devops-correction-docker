- name: Launch proxy
  docker_container:
    name: httpd
    image: denila/tp-devops-httpd:latest
    ports:
      - "80:80"
    networks:
      - name: my-network
  vars:
    ansible_python_interpreter: /opt/docker_venv/bin/python